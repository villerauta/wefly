using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModstationPlane : MonoBehaviour
{
    public AirplaneAssembler mockPlane;
    // Start is called before the first frame update
    public void Init()
    {
        //mockPlane.reassemble();
    }
}
